# terminology
* **Disk I/O Operations**: These are write and read operations that involve accessing the computer disk
* **Records**: a row in database table maps to a record on computer's disk. There are different types of records. Data records, index records and records to metadat