**Khi** tôi **đang tối ưu hóa** mã cho chức năng tìm kiếm, tôi **đã nhận ra** rằng thuật toán của chúng tôi **rất chậm**. Chúng tôi **đã sử dụng** một cấu trúc dữ liệu không hiệu quả. Tôi **phát hiện ra** rằng nếu chúng tôi **đã chuyển sang** sử dụng **B-tree** thay vì danh sách liên kết, tốc độ tìm kiếm **đã có thể** nhanh hơn gấp mười lần. Tôi **đã mất** ba ngày để viết lại hoàn toàn logic tìm kiếm.


**Năm tới**, chúng tôi **sẽ di chuyển** toàn bộ cơ sở hạ tầng dữ liệu lên Google Cloud Platform (GCP). Chúng tôi **sắp sửa sử dụng** BigQuery làm nền tảng chính cho phân tích. Tôi **sẽ viết** các tập lệnh Python để tự động hóa quá trình di chuyển dữ liệu thô. Tôi **hy vọng** rằng việc chuyển đổi này **sẽ cho phép** các nhà khoa học dữ liệu (data scientists) của chúng tôi chạy các mô hình Machine Learning nhanh hơn nhiều. Chúng tôi **sẽ không tiếp tục** dựa vào máy chủ tại chỗ (on-premise servers).

 when i was optimizing the code for search feature, i realized that our algorithm was very slow. We had used an inefficient data structure. I discovered that if we had used b-tree instead of the linked list, search performance could have been 10 times faster. I spent 3 days to rewrite new search logic.

next year, we will migrate all o